# PostgreSQL: пользователь и пароль (создаётся при первом запуске)
DB_USER=weather_user
DB_PASSWORD=weather_secret_change_me

# Имена баз данных
DB_NAME=weather_analytics
REDASH_DB_NAME=redash_app

# Redash
REDASH_COOKIE_SECRET=weather-analytics-redash-secret-change-in-production
REDASH_PORT=5000

# Хост БД внутри Docker-сети (имя сервиса в docker-compose)
DB_HOST=postgres
